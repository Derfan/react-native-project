import React from "react";
import { View, Text, StyleSheet } from "react-native";

const GuessingGame = () => (
  <View>
    <Text>GuessingGame v1</Text>
  </View>
);

GuessingGame.navigationOptions = {
  title: 'GuessingGame'
};

const styles = StyleSheet.create({});

export default GuessingGame;
